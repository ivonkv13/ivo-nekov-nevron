export interface ClearNumbersResponse {
    numbers: number[];
    sum: number;
  }
  
  export interface GenerateNumbersResponse {
    numbers: number[];
  }
  
  export interface GetNumbersResponse {
    numbers: number[];
  }
  
  export interface SumNumbersResponse {
    sum: number;
  }